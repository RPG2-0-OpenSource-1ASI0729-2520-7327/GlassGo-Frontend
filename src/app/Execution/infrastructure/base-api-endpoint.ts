/**
 * Base class for API endpoint configurations in the Execution bounded context.
 * Provides common base URLs and endpoint patterns for infrastructure services.
 */
export class BaseApiEndpoint {
  /** Base URL for the API server. */
  static readonly BASE_URL = 'http://localhost:3000'; // JSON Server URL
}
